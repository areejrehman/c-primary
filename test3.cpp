#include <iostream>
using namespace std;
int main()
{
    int no = 2, sum = 0;
          while (no <= 10);
{
     sum += no; // sum = sum + no
     ++no;
}
cout << "Sum of 2 to 10 =" << sum << endl;
return 0;
}